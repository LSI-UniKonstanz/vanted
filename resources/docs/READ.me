The ant script javadoc.xml is used for generating Vanted documentation. To run the script use

	ant

The generated output should later be uploaded to the webserver in the respective directory. 
To edit any meta-information, see the properties of the scipt. Optionally, to introduce new
tags, add a new tag specification on a new line. For attributes' reference:
https://docs.oracle.com/javase/7/docs/technotes/tools/windows/javadoc.html#tag
